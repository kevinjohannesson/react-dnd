export default function resetElement(element: HTMLElement) {
  element.style.position = '';
  element.style.top = '';
  element.style.left = '';

  element.style.zIndex = '';
  
  element.style.transform = '';
  
  element.style.transition = '';
}